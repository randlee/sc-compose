# sc-compose

Standalone workspace for:
- `sc-composer`
- `sc-compose`

This repo is intended to replace the in-workspace `sc-composer` and
`sc-compose` crates currently published from `agent-team-mail`.
