---
id: fuzz-run-report-shell-template
title: Top-Level Report Shell Template (fuzz-run-report.html.j2)
status: complete
branch: feature/fuzz-run-report-template
worktree: ../sc-compose-worktrees/feature/fuzz-run-report-template
target: develop
---

# Sprint — Top-Level Report Shell Template

## Goal

Today only the per-worker panel has a checked-in Jinja2 template
(`.claude/skills/html-report/templates/fuzz-run-agent.xhtml.j2`). The
top-level report shell (header, session-summary table, composed worker
sections, footer, metadata table) is hand-assembled by the
`html-report-generator` background agent directly from prose instructions in
`.claude/agents/html-report-generator.md`, with no checked-in template behind
it. `~/.claude/skills/html-report/SKILL.md` already states the canonical
workflow should "prefer `sc-compose render` with Jinja2 templates over ad-hoc
HTML generation" — the shell currently does not follow that.

This sprint adds a second template that owns the top-level shell, so the full
report — not just the worker panel — is deterministically generated by
`sc-compose render` from a JSON var-file, with zero hand-assembled or
ad-hoc-generated markup.

## Variable Contract

New template: `.claude/skills/html-report/templates/fuzz-run-report.html.j2`

Confirmed via direct testing against the installed `sc-compose` CLI
(`v1.2.0`) that both shapes below render cleanly through
`sc-compose render --var-file` today — no product bug blocks this contract:

- `rows`: **top-level** array of objects, one per fuzz-run row in the
  session-summary table, e.g.
  `{"label": "...", "iterations": 99, "pass": "99/99", "result": "PASS"}`.
  (Top-level array-of-objects has shipped since PR #50 / FR-13.)
- `sections`: **top-level** array of strings — each string is one worker
  panel's already-rendered XHTML (the output of rendering
  `fuzz-run-agent.xhtml.j2` once per worker), inserted verbatim into the
  shell. This keeps `sections` a flat array of scalars, not an array of
  objects or nested arrays, so it is unaffected by the still-open nested-path
  array-of-objects gap (tracked separately, see Non-Goals).

Required top-level scalar variables (mirroring
`~/.claude/skills/html-report/SKILL.md`'s Input Contract naming where it
overlaps): `title`, `subtitle`, `generated_at`, `status`, `summary_intro_html`
(the prose paragraph(s) above the table), `rows`, `sections`,
`recommendations_html` (optional), `footer_html` (optional), and a
`metadata_rows` top-level array of objects (same shape rule as `rows`) for
the closing metadata table.

## Exact Targets

1. Create `.claude/skills/html-report/templates/fuzz-run-report.html.j2`
   rendering the full self-contained HTML document per the structure already
   documented in `.claude/agents/html-report-generator.md`'s "Report
   Structure" section (DOCTYPE, `<meta charset="UTF-8">`, inline CSS only,
   header/summary/sections/recommendations/footer order), using the variable
   contract above. Preserve the existing CSS palette and copy-button markup
   already established for this report family (see
   `site/reports/20260729-3-fuzz-report.html` for the current visual
   baseline — palette, `.session-summary` table styling, copy-button
   behavior).
2. Update `.claude/agents/html-report-generator.md` so that, for report
   families that supply a top-level shell template path (or where one exists
   for the family in question), the agent renders the shell via
   `sc-compose render --file <shell-template> --var-file <var-file>` instead
   of hand-assembling the HTML document itself. The per-worker
   `fuzz-run-agent.xhtml.j2` rendering step is unchanged.
3. Update `~/.claude/skills/html-report/SKILL.md` and
   `.claude/skills/html-report/fuzz-run-agent-contract.md` to document the
   two-template split (worker-panel template + shell template) as the
   pattern for this report family, and note the array-shape rule (top-level
   arrays only; pre-render nested content to strings) as general guidance for
   any future report family hitting the same constraint.
4. Regenerate `site/reports/20260729-3-fuzz-report.html` (same underlying
   data already in `site/reports/20260729-3-fuzz-report/` — do not change
   the JSON sidecar's schema or the worker panels' content) through the new
   two-template pipeline, confirming the rendered output is content-identical
   to the current file modulo the shell now being template-generated.

## Non-Goals

- Do not attempt to fix the nested-path array-of-objects `sc-compose`
  limitation (`agent.test_inputs` and similar) — that gap is tracked
  separately and is not required for this contract, since `rows`,
  `sections`, and `metadata_rows` are all top-level arrays.
- Do not change the worker-panel template's markup or variable contract
  beyond what already landed in commit `4910eca` on this branch.
- Do not touch `20260729-1-fuzz-report.*` or `20260729-2-fuzz-report.*` in
  this sprint — regenerate only `20260729-3-fuzz-report.*` as the proof case.
- Do not build a website index page.

## Acceptance Criteria

- `.claude/skills/html-report/templates/fuzz-run-report.html.j2` exists and
  renders successfully via `sc-compose render --file ... --var-file ...`
  using a var-file with top-level `rows` (array of objects) and `sections`
  (array of strings) — attach the exact command and its exit code as
  evidence.
- `xmllint`/`html-validate` (per this repo's existing validation gates) pass
  on the regenerated `site/reports/20260729-3-fuzz-report.html`.
- `.claude/agents/html-report-generator.md` documents rendering the shell via
  `sc-compose render` against the new template rather than ad-hoc assembly.
- No hand-edited HTML anywhere in the regenerated output — the sprint doc's
  Completion Evidence must state the exact `sc-compose render` commands used
  to produce every artifact touched.
- sprint doc deliverables gate:
  - this doc exists
  - this doc's frontmatter sets `status: complete`
- this doc's frontmatter sets `branch:` and `worktree:`

## Completion Evidence

The shell template rendered successfully from a generated var-file containing
four top-level `rows` objects, four top-level `sections` strings, and eleven
top-level `metadata_rows` objects. The exact proof command completed with exit
code 0:

```bash
target/debug/sc-compose render --root . \
  --file .claude/skills/html-report/templates/fuzz-run-report.html.j2 \
  --var-file /tmp/sc-compose-fuzz-run-shell-vars.json \
  --output site/reports/20260729-3-fuzz-report.html
```

The var-file was derived from
`site/reports/20260729-3-fuzz-report/20260729-3-fuzz-report.json` and the
existing four worker panel artifacts. No worker panel or JSON sidecar was
rewritten. The only generated report artifact touched by this sprint was
`site/reports/20260729-3-fuzz-report.html`, and it was produced by the
`sc-compose render` command above rather than hand-edited.
