# title: Compatibility matrix
# sets:
#   - publish
#   - test
# note:
#   kind: backward-compat
Existing bundled examples and edge-case fixtures stay in the shared proof.

Optional fields may be absent, extra metadata is ignored, and large payload
inputs still render deterministically.
